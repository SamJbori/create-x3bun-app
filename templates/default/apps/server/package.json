{
  "name": "@x3bun/server",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot --env-file=../../.env --port=3006 src/index.ts ",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint --flag unstable_native_nodejs_ts_config",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/trpc-server": "config",
    "@t3-oss/env-core": "config",
    "@x3bun/api": "workspace:*",
    "bun": "config",
    "hono": "config",
    "mongodb": "config",
    "zod": "config"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "config",
    "@types/bun": "config",
    "@types/node": "config",
    "@x3bun/eslint": "workspace:*",
    "@x3bun/prettier": "workspace:*",
    "@x3bun/typescript": "workspace:*",
    "typescript": "config"
  },
  "prettier": "@x3bun/prettier/base"
}
